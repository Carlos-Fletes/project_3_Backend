package com.example.rest_service;

import org.springframework.stereotype.Controller;

// Removed callback endpoint; OAuth2LoginSuccessHandler handles redirect now
@Controller
public class OAuthController {
    // No methods needed here for Expo redirect
}
