spring.application.name=rest-service
server.port=8080

# Supabase Configuration - Using environment variables for Heroku with defaults for local development
supabase.url=${SUPABASE_URL:https://vyxaqysujjmhjsaonszw.supabase.co}
supabase.key=${SUPABASE_ANON_KEY}
supabase.service-role-key=${SUPABASE_SERVICE_ROLE_KEY}

# Server Configuration
server.port=${PORT:8080}

# JSON Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.deserialization.fail-on-unknown-properties=false

# Android GitHub OAuth app (client ID: Ov23li1MaKxzTImvJ2lK)
github.android.client.id=${GITHUB_ANDROID_CLIENT_ID:${GITHUB_CLIENT_ID}}
github.android.client.secret=${GITHUB_ANDROID_CLIENT_SECRET:${GITHUB_CLIENT_SECRET}}
github.android.redirect.uri=${GITHUB_ANDROID_REDIRECT_URI:com.cfletes.project3fe://}

# Web GitHub OAuth app (client ID: Ov23lidXmzwFSQlo0Iir)
github.web.client.id=${GITHUB_WEB_CLIENT_ID}
github.web.client.secret=${GITHUB_WEB_CLIENT_SECRET}
github.web.redirect.uri=${GITHUB_WEB_REDIRECT_URI:http://localhost:8081/auth/github/callback}
